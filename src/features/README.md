# Product features

Each product capability lives in its own directory here and exposes its public
surface through that feature's `index.ts`. Feature implementation starts in
Task 002; this file preserves the source boundary without adding a placeholder
runtime abstraction.

See [the architecture documentation](../../docs/architecture.md) for dependency
and ownership rules.
